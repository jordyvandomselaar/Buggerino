import React from "react";

import {Text} from "react-native-elements"

interface IProps {

}

const OrganizationSelector = ({}: IProps) => {
    return (
        <Text>Hol</Text>
    );
};

export default OrganizationSelector;
